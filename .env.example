# Пример переменных окружения для anatoly-game
# Копируйте в .env.local или .env.production при необходимости

# API базовый URL (если потребуется)
VITE_API_BASE_URL=https://api.game.znak.pics

# Окружение
VITE_ENV=production

# Логирование
VITE_LOG_LEVEL=info

# Analytics (если используется)
VITE_ANALYTICS_ID=

# Версия приложения
VITE_APP_VERSION=1.0.0

# Домен приложения
VITE_APP_DOMAIN=game.znak.pics

# Примечание: переменные префиксом VITE_ будут доступны в браузере через import.meta.env
# Переменные без VITE_ префикса будут доступны только во время сборки
